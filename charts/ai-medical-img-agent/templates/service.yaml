apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-medical-img-agent.fullname" . }}
  labels:
    {{- include "ai-medical-img-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-medical-img-agent.selectorLabels" . | nindent 4 }}