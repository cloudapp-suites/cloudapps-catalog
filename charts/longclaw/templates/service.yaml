apiVersion: v1
kind: Service
metadata:
  name: {{ include "longclaw.fullname" . }}
  labels:
    {{- include "longclaw.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "longclaw.selectorLabels" . | nindent 4 }}
