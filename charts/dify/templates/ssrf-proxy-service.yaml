apiVersion: v1
kind: Service
metadata:
  name: {{ include "dify.fullname" . }}-ssrf-proxy
  labels:
    {{- include "dify.labels" . | nindent 4 }}
    app.kubernetes.io/component: ssrf-proxy
spec:
  type: {{ .Values.ssrfProxy.service.type }}
  ports:
    - port: {{ .Values.ssrfProxy.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dify.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: ssrf-proxy