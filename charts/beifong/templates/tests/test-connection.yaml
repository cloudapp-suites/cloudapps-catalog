apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "beifong.fullname" . }}-test-connection"
  labels:
    {{- include "beifong.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "beifong.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
