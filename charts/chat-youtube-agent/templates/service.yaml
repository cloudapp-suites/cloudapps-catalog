apiVersion: v1
kind: Service
metadata:
  name: {{ include "chat-youtube-agent.fullname" . }}
  labels:
    {{- include "chat-youtube-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chat-youtube-agent.selectorLabels" . | nindent 4 }}