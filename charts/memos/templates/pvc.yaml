apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: memos-data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.memos.storageClassName }}
  resources:
    requests:
      storage: {{ .Values.memos.storageSize }}