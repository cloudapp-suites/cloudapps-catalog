apiVersion: v1
kind: Service
metadata:
  name: {{ include "copaw.fullname" . }}
  labels:
    {{- include "copaw.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort | default .Values.containerPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "copaw.selectorLabels" . | nindent 4 }}
