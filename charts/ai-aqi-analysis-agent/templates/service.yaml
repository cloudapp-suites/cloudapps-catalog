apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-aqi-analysis-chart.fullname" . }}
  labels:
    {{- include "ai-aqi-analysis-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-aqi-analysis-chart.selectorLabels" . | nindent 4 }}
