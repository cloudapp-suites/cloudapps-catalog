{{- if eq .Values.frontend.service.type "LoadBalancer" }}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: "{{ include "agentgpt.fullname" . }}-ip-updater-binding"
  labels:
    {{- include "agentgpt.labels" . | nindent 4 }}
subjects:
- kind: ServiceAccount
  name: "{{ include "agentgpt.fullname" . }}-ip-updater"
  namespace: {{ .Release.Namespace }}
roleRef:
  kind: Role
  name: "{{ include "agentgpt.fullname" . }}-ip-updater-role"
  apiGroup: rbac.authorization.k8s.io
{{- end }}