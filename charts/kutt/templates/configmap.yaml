apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kutt.fullname" . }}-config
  labels:
    {{- include "kutt.labels" . | nindent 4 }}
data:
  # 默认域名将在部署后通过脚本更新
  default-domain: {{ .Values.env.DEFAULT_DOMAIN | quote }}