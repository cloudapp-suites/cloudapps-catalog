apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-audio-tour-chart.fullname" . }}
  labels:
    {{- include "ai-audio-tour-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "ai-audio-tour-chart.selectorLabels" . | nindent 4 }}
