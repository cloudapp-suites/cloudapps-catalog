apiVersion: v1
kind: Service
metadata:
  name: {{ include "blog-to-podcast-agent.fullname" . }}
  labels:
    {{- include "blog-to-podcast-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "blog-to-podcast-agent.selectorLabels" . | nindent 4 }}