apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "blog-to-podcast-agent.fullname" . }}
  labels:
    {{- include "blog-to-podcast-agent.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "blog-to-podcast-agent.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "blog-to-podcast-agent.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8501
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
          readinessProbe:
            httpGet:
              path: /
              port: http
          resources:
            {{- toYaml .Values.resources | nindent 12 }}