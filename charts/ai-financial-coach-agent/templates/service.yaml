apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-financial-coach-chart.fullname" . }}
  labels:
    {{- include "ai-financial-coach-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-financial-coach-chart.selectorLabels" . | nindent 4 }}
