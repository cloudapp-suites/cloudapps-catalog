apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "openclaw.fullname" . }}-config
  labels:
    {{- include "openclaw.labels" . | nindent 4 }}
data:
  bash_aliases: |
    alias openclaw='node /app/dist/index.js'
  openclaw.json: |
    {{- .Values.externalEnv.openclaw_json | nindent 4 }}