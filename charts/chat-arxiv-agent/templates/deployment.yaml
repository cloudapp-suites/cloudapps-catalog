apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "chat-arxiv-agent.fullname" . }}
  labels:
    {{- include "chat-arxiv-agent.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "chat-arxiv-agent.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "chat-arxiv-agent.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8501
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
          readinessProbe:
            httpGet:
              path: /
              port: http
          resources:
            {{- toYaml .Values.resources | nindent 12 }}