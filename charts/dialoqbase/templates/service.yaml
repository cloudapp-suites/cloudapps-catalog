apiVersion: v1
kind: Service
metadata:
  name: {{ include "dialoqbase.fullname" . }}
  labels:
    {{- include "dialoqbase.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dialoqbase.appSelectorLabels" . | nindent 4 }}