apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cherry-studio-enterprise.fullname" . }}-test-connection"
  labels:
    {{- include "cherry-studio-enterprise.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cherry-studio-enterprise.fullname" . }}:{{ .Values.app.api.service.port }}']
