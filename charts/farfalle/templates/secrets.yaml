apiVersion: v1
kind: Secret
metadata:
  name: farfalle-secrets
type: Opaque
data:
  # Add any secret values here if needed
  # Format: key: base64-encoded-value
  # Example: postgres-password: {{ .Values.database.password | b64enc }}