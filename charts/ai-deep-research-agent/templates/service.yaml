apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-deep-research-agent.fullname" . }}
  labels:
    {{- include "ai-deep-research-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-deep-research-agent.selectorLabels" . | nindent 4 }}